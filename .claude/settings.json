{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"${TOOL_INPUT_FILE_PATH}\"; echo \"$f\" | grep -qE '(api/|actions/).*\\.ts' && ! grep -qE '(from.*zod|require.*zod|\\.parse\\(|\\.safeParse\\()' \"$f\" 2>/dev/null && grep -qE '(POST|PUT|PATCH|DELETE|use server)' \"$f\" 2>/dev/null && echo '{\"feedback\": \"⚠️ Mutation endpoint without Zod validation.\"}' || true",
            "description": "Flag API routes and server actions missing Zod validation"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"${TOOL_INPUT_FILE_PATH}\"; grep -qiE 'NEXT_PUBLIC_.*SERVICE_ROLE|NEXT_PUBLIC_.*SECRET' \"$f\" 2>/dev/null && echo '{\"feedback\": \"🔴 Sensitive key exposed with NEXT_PUBLIC_ prefix.\"}' || true",
            "description": "Block NEXT_PUBLIC_ exposure of secrets"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"${TOOL_INPUT_FILE_PATH}\"; grep -qE 'Access-Control-Allow-Origin.*\\*' \"$f\" 2>/dev/null && echo '{\"feedback\": \"⚠️ CORS wildcard origin detected. Use explicit allowlist.\"}' || true",
            "description": "Flag CORS wildcard origins"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"${TOOL_INPUT_FILE_PATH}\"; echo \"$f\" | grep -qE '(components/|app/.*page\\.|app/.*layout\\.)' && grep -qiE 'service_role' \"$f\" 2>/dev/null && echo '{\"feedback\": \"🔴 service_role key referenced in client-side file. This key bypasses RLS and must never be exposed to the browser.\"}' || true",
            "description": "Block service_role key usage in client-side files"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"${TOOL_INPUT_COMMAND}\" | grep -qE 'supabase.*db.*push' && echo \"${TOOL_INPUT_COMMAND}\" | grep -qvE 'staging|dev|preview|local' && echo '{\"feedback\": \"⚠️ Migration push detected — verify this is not production.\"}' || true",
            "description": "Warn before pushing migrations"
          }
        ]
      }
    ]
  }
}
